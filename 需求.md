交易系统全栈开发任务书 (React + Go)
任务目标：
开发一个响应式的 Web 交易终端，模仿专业级金融软件界面。系统包含 1 分钟周期 K 线主图及下方的成交量副图。

一、 技术栈要求
后端： Go 语言 (Golang)，使用 Gin 框架处理 REST API，Gorilla WebSocket 处理实时流数据。

前端： React.js，状态管理推荐使用 Hooks (useContext/useReducer)。

可视化库： 使用 Lightweight Charts (TradingView) 或 ECharts。

二、 数据协议与后端逻辑 (Go)
数据结构： 定义 Tick 结构体包含：Time (Unix), Open, High, Low, Close, Volume。

API 端点：

GET /history：返回最近 200-500 个周期的历史 K 线数据。

WS /ws：建立双工长连接。

实时逻辑：

后端维护一个内存池，每秒产生一次随机波动的价格（模拟真实交易）。

每隔 60 秒，将当前分钟的 Tick 封装成一个新的 K 线柱，通过 WebSocket 推送至前端。

三、 前端视觉与布局规格 (React)
整体风格： 极致暗黑模式。背景色 #000000，文字色 #D1D4DC。

布局划分：

容器高度： 100vh。

主图窗格 (Upper Pane)： 占比 75%。显示 1 分钟 K 线。

副图窗格 (Lower Pane)： 占比 25%。显示成交量柱状图。

配色逻辑：

上涨 (Up)： 红色 (#FF5252)，实心。

下跌 (Down)： 青色 (#00FFFF)，实心。

技术指标：

主图叠加： MA5（黄色）和 MA10（白色）两条平滑移动平均线。

副图指标： 成交量柱状图颜色须随当期 K 线涨跌联动，并叠加 5 日量能均线（紫色）。

四、 交互细节 (Interaction)
同步联动： 缩放（Zooming）或平移（Panning）主图时，副图必须保持 X 轴完全同步滚动。

十字光标： 鼠标悬停时，双图同步显示十字垂直引导线，并在坐标轴上弹出当前数值标签。

实时更新： 前端收到 WebSocket 数据后，必须平滑追加最新的价格点，不得刷新整个图表。

五、 输出要求
请提供后端的核心 WebSocket 推送逻辑代码 (main.go)。

请提供前端 React 组件的结构，特别是如何初始化图表、配置配色方案以及处理 WebSocket 增量更新的代码。

六、 项目启动命令

后端（Go + air 热重载）：

```bash
cd back && air -c .air.toml
```

- 默认监听端口：`8080`
- 首次编译约需 30-40 秒
- 修改 `.go` 文件后自动重新编译

前端（Vite + React）：

```bash
cd front && npm run dev
```

- 默认访问地址：`http://localhost:4000`
- 修改代码后 HMR 热更新
- 代理配置（`vite.config.js`）：`/history`、`/refresh`、`/grid-search` → `localhost:8080`，`/ws` → `ws://localhost:8080`
