# 交易系统算法说明

## 一、KAMA（考夫曼自适应移动平均线）计算

### 1.1 算法原理

KAMA（Kaufman's Adaptive Moving Average）由 Perry Kaufman 提出，是一种**根据市场噪音程度自动调节灵敏度**的移动平均线。市场趋势明显时贴近价格（快反应），市场震荡时远离价格（慢反应），从而避免假信号。

### 1.2 计算步骤

设当前为第 `i` 根 K 线收盘价为 `close[i]`，ER 周期为 `period`：

#### 步骤 1：计算价格变动（方向性变动）

```
change = |close[i] - close[i - period]|
```

即当前收盘价与 `period` 根 K 线前的收盘价的**绝对差值**，代表这段时间内的净方向性移动。

#### 步骤 2：计算波动率（累积变动）

```
volatility = Σ |close[j] - close[j-1]|  (j 从 i-period+1 到 i)
```

即 `period` 根 K 线内**每一根相邻K线收盘价差值的绝对值之和**，代表这段时间内的总波动幅度（含来回震荡）。

#### 步骤 3：计算效率比率（Efficiency Ratio, ER）

```
if volatility == 0:
    ER = 0
else:
    ER = change / volatility
```

- `ER` 的取值范围为 `[0, 1]`。
- `ER ≈ 1`：价格单向运动，几乎无噪音（强趋势）。
- `ER ≈ 0`：价格来回震荡，无方向（横盘）。

#### 步骤 4：计算平滑常数（Smoothing Constant, SC）

```
fastSC = 2 / (fastSmooth + 1)     // 快速平滑系数，fastSmooth 通常取 2
slowSC = 2 / (slowSmooth + 1)     // 慢速平滑系数，slowSmooth 通常取 30
SC = (ER × (fastSC - slowSC) + slowSC)²
```

- 当 `ER = 1` 时，`SC = fastSC²`（最快反应）。
- 当 `ER = 0` 时，`SC = slowSC²`（最慢反应）。
- 通过平方操作放大快慢差异，使趋势与横盘的区分更加明显。

#### 步骤 5：计算 KAMA 值

```
KAMA[i] = KAMA[i-1] + SC × (close[i] - KAMA[i-1])
```

- 初始值：从第 `period` 根 K 线开始计算，`KAMA[period-1] = close[period-1]`。
- 前 `period-1` 根 K 线的值为 `NaN`（数据不足）。

### 1.3 快线与慢线

系统使用**两条不同 ER 周期**的 KAMA 线形成交叉策略：

| 参数 | 快线 KAMA | 慢线 KAMA |
|------|-----------|-----------|
| ER 周期 | `erFastPeriod`（网格搜索范围 3~30） | `erSlowPeriod`（网格搜索范围 10~60） |
| 快速平滑周期 | `fastSmooth = 2`（固定） | `fastSmooth = 2`（固定） |
| 慢速平滑周期 | `slowSmooth`（网格搜索 10/20/30/40） | `slowSmooth`（同左） |

**核心差异**：快线使用较小的 ER 周期（如 5），对近期价格变化更敏感；慢线使用较大的 ER 周期（如 30），更平滑稳定。两条线共享相同的 `fastSmooth` 和 `slowSmooth` 参数。

### 1.4 交叉信号检测

```
// 金叉（做多信号）：快线从下方穿越慢线
fastKAMA[i-1] <= slowKAMA[i-1] 且 fastKAMA[i] > slowKAMA[i]

// 死叉（做空信号）：快线从上方穿越慢线
fastKAMA[i-1] >= slowKAMA[i-1] 且 fastKAMA[i] < slowKAMA[i]
```

- 跳过任何 `NaN` 位置（初始数据不足阶段）。
- 每个交叉点记录 `Index`（K线索引）、`FastKAMA`、`SlowKAMA` 和 `IsLong`（是否为金叉）。

---

## 二、网格搜索（Grid Search）算法

### 2.1 目的

通过**穷举遍历**所有 KAMA 快慢线参数组合，进行历史回测，找到期望值最优的参数组合。

### 2.2 参数搜索空间

```
Fast（快线 ER 周期）:  3, 4, 5, ..., 30     → 28 个值
Slow（慢线 ER 周期）: 10, 11, 12, ..., 60   → 51 个值
SlowSmooth（慢速平滑周期）: [10, 20, 30, 40] → 4 个值
FastSmooth（快速平滑周期）: 2（固定）
```

**约束条件**：`Fast < Slow`（快线周期必须小于慢线周期，否则过滤）。

**总组合数**：约 28 × 51 × 4 ≈ 5712 组（扣除 `Fast >= Slow` 的无效组合后实际更少）。

### 2.3 回测逻辑（backtest 函数）

对每组参数，执行以下回测流程：

#### 2.3.1 信号生成

调用 `DetectKAMACross(closes, fastPeriod, slowPeriod, 2, slowSmooth)` 获取所有交叉信号列表。

#### 2.3.2 交易执行规则

| 当前持仓 | 收到信号 | 操作 |
|----------|----------|------|
| 空仓 (0) | 金叉 | 开多仓，记录入场价 |
| 空仓 (0) | 死叉 | 开空仓，记录入场价 |
| 持多仓 (1) | 死叉 | **平多仓**（按信号价结算），收益计入资金 → **反手开空仓** |
| 持空仓 (-1) | 金叉 | **平空仓**（按信号价结算），收益计入资金 → **反手开多仓** |

**资金管理**：初始资金 `cash = 1.0`，每笔交易的收益率复利累加：

```
// 多仓收益
cash = cash × (1 + (sellPrice - entryPrice) / entryPrice)

// 空仓收益
cash = cash × (1 + (entryPrice - sellPrice) / entryPrice)
```

#### 2.3.3 强制平仓

如果回测结束时仍有持仓，以最后一根 K 线的收盘价强制平仓。

#### 2.3.4 统计指标

每组合返回以下指标：

- **总收益率（TotalReturn）**：`(cash - 1.0)`，复利累计后的净收益率。
- **交易次数（NumTrades）**：完成的交易笔数。
- **胜率（WinRate）**：`盈利交易数 / 总交易数`。
- **期望值（Expectancy）**：`胜率 × 平均盈利 - 败率 × 平均亏损`。

```
Expectancy = WinRate × AvgWin - (1 - WinRate) × AvgLoss
```

其中 `AvgWin` 为盈利交易的平均收益率，`AvgLoss` 为亏损交易的平均亏损绝对值。

### 2.4 结果排序与输出

- 所有组合按 **期望值降序** 排列。
- 取前 **20 名** 作为返回结果。
- 单独记录期望值最高的组合作为 **Best**，并附带其全部交易明细。

### 2.5 API 接口

```
GET /grid-search
```

**响应结构**：

```json
{
  "results": [  // 前20名参数组合
    {
      "fast": 5,
      "slow": 20,
      "slowSmooth": 30,
      "totalReturn": 0.15,
      "numTrades": 42,
      "winRate": 0.65,
      "expectancy": 0.003
    }
  ],
  "best": {  // 期望值最高的组合
    "fast": 5,
    "slow": 20,
    "slowSmooth": 30,
    "totalReturn": 0.15,
    "numTrades": 42,
    "winRate": 0.65,
    "expectancy": 0.003
  },
  "trades": [  // 最优组合的交易明细
    {
      "buyTime": 1718467200,
      "buyPrice": 1.085,
      "sellPrice": 1.092,
      "return": 0.0065,
      "direction": "long"
    }
  ]
}
```
